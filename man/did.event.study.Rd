% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bjs.R
\name{did.event.study}
\alias{did.event.study}
\title{Event study via the BJS imputation estimator}
\usage{
did.event.study(
  data,
  yname,
  gname,
  tname,
  idname,
  pre.window,
  post.window,
  cluster.var = NULL,
  trends.lin = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{data}{Data frame or data.table.}

\item{yname}{Outcome variable name (string).}

\item{gname}{Treatment-timing variable name; never-treated coded as
\code{NA}/\code{Inf}/non-positive (see \code{\link[=is_never_treated]{is_never_treated()}}).}

\item{tname}{Time-period variable name.}

\item{idname}{Unit identifier.}

\item{pre.window}{Pre-treatment event times (e.g. \code{-4:-1}).}

\item{post.window}{Post-treatment event times (e.g. \code{0:3}).}

\item{cluster.var}{Clustering variable (default: \code{idname}).}

\item{trends.lin}{If \code{TRUE}, pre-residualize the outcome on unit-specific
pre-treatment linear trends before estimation (default \code{FALSE}).}

\item{verbose}{If \code{TRUE} (default), print a console summary.}
}
\value{
A \code{taylorDiD_es} result object (list) with \code{coefficients},
\code{pre.mean.treated}, \code{pretrends.test}, \code{avg.post.effect}, \code{n.obs},
\code{n.groups}, \code{n.treated}.
}
\description{
Estimates coefficient-by-event-time effects, a joint pre-trends F-test, and a
post-treatment average effect (auxiliary static DiD on the never-/not-yet-/
treated-in-window subset, per house style). With \code{trends.lin = TRUE} the
outcome is first pre-residualized on unit-specific linear time trends fit from
pre-treatment observations.
}
\examples{
panel <- readRDS(system.file("extdata", "synthetic_panel.rds",
                             package = "taylorDiD"))
\donttest{
es <- did.event.study(panel, "y", "g", "year", "id",
                      pre.window = -4:-1, post.window = 0:3)
es$avg.post.effect
}
}
