% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{demographic.table.output}
\alias{demographic.table.output}
\title{LaTeX regression table from a list of lm() models}
\usage{
demographic.table.output(
  models,
  model.names = NULL,
  var.names = NULL,
  decimal.places = 3,
  output.file = NULL
)
}
\arguments{
\item{models}{List of \code{lm()} (or compatible) model objects.}

\item{model.names}{Optional column titles (\code{NULL} = no title row).}

\item{var.names}{Optional named vector mapping variable names to display
labels (its order sets the row order).}

\item{decimal.places}{Coefficient/SE precision (default 3).}

\item{output.file}{Optional path; if given, the LaTeX is written there.}
}
\value{
The LaTeX string, invisibly.
}
\description{
General-purpose coefficient table: one column per model, coefficient over
standard error with significance stars, and an N row. Variable display names
and ordering follow \code{var.names} when supplied.
}
\examples{
m1 <- lm(mpg ~ wt, data = mtcars)
m2 <- lm(mpg ~ wt + hp, data = mtcars)
cat(demographic.table.output(list(m1, m2),
                             model.names = c("(a)", "(b)")))
}
