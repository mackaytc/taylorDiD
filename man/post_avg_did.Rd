% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event-study.R
\name{post_avg_did}
\alias{post_avg_did}
\title{Post-treatment average effect via an auxiliary static DiD}
\usage{
post_avg_did(
  data,
  yname,
  gname,
  tname,
  idname,
  post.window,
  cluster.var = idname,
  pre.mean.treated = NA_real_,
  estimator = c("bjs", "did2s")
)
}
\arguments{
\item{data}{Data frame or data.table (already detrended if applicable).}

\item{yname, gname, tname, idname}{Column names (strings).}

\item{post.window}{Integer vector of post-treatment event times; only
\code{max(post.window)} is used to bound the window.}

\item{cluster.var}{Clustering variable (default: \code{idname}).}

\item{pre.mean.treated}{Pre-treatment mean, used to express the percent change.}

\item{estimator}{One of \code{"bjs"} or \code{"did2s"}.}
}
\value{
List with \code{estimate}, \code{std.error}, \code{p.value}, \code{pct.change}, \code{window}.
}
\description{
Computes the summary post-treatment effect by fitting a static DiD on the
subset of never-treated, not-yet-treated, and treated-within-window
observations (\code{event_time \%in\% 0:max(post.window)}), rather than averaging the
event-study coefficients. Supported for the imputation (\code{"bjs"}) and two-stage
(\code{"did2s"}) estimators.
}
\keyword{internal}
