% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_event_study}
\alias{plot_event_study}
\title{Event-study plot with house defaults}
\usage{
plot_event_study(
  result,
  y.title = NULL,
  x.title = "Event Time",
  title = NULL,
  caption = NULL,
  point.fill = "coral3"
)
}
\arguments{
\item{result}{A \code{taylorDiD_es} object (uses its \code{coefficients} table, which
must carry \code{event.time}, \code{estimate}, \code{conf.low}, \code{conf.high}).}

\item{y.title}{Optional y-axis title (default none).}

\item{x.title}{X-axis title (default \code{"Event Time"}; pass \code{NULL} to omit).}

\item{title}{Optional plot title.}

\item{caption}{Optional caption (used for diagnostic annotations).}

\item{point.fill}{Fill color for the point estimates (default \code{"coral3"}).}
}
\value{
A \code{ggplot} object.
}
\description{
Point estimates with 95\% confidence-interval errorbars, a dashed zero line,
and a serif theme, with integer breaks on the event-time axis.
}
\examples{
es <- new_es_result(
  coefficients = data.table::data.table(
    event.time = -2:2, term = as.character(-2:2),
    estimate = c(0.02, -0.01, 0.30, 0.42, 0.55), std.error = rep(0.1, 5),
    p.value = c(0.8, 0.9, 0.01, 0.001, 0.001),
    conf.low = c(-0.18, -0.21, 0.10, 0.22, 0.35),
    conf.high = c(0.22, 0.19, 0.50, 0.62, 0.75)),
  pre.mean.treated = 2.0,
  pretrends.test = list(f.stat = 0.3, df = 2, p.value = 0.74),
  avg.post.effect = list(estimate = 0.42, std.error = 0.1, p.value = 0.001,
                         pct.change = 21, window = c(0, 2)),
  n.obs = 500L, n.groups = 50L, n.treated = 20L, estimator = "bjs")
plot_event_study(es, y.title = "Effect on outcome")
}
