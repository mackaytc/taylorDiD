% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/did2s.R
\name{did2s_event_study}
\alias{did2s_event_study}
\title{Event study via the Gardner two-stage estimator}
\usage{
did2s_event_study(
  data,
  yname,
  gname,
  tname,
  idname,
  pre.window,
  post.window,
  cluster.var = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{Data frame or data.table.}

\item{yname}{Outcome variable name (string).}

\item{gname}{Treatment-timing (cohort) variable; never-treated coded as
\code{NA}/\code{Inf}/non-positive.}

\item{tname}{Time-period variable name.}

\item{idname}{Unit identifier.}

\item{pre.window}{Pre-treatment event times (e.g. \code{-4:-1}).}

\item{post.window}{Post-treatment event times (e.g. \code{0:3}).}

\item{cluster.var}{Clustering variable (default: \code{idname}).}

\item{verbose}{If \code{TRUE} (default), print a console summary.}
}
\value{
A \code{taylorDiD_es} result object; also carries the \code{prepared} panel
list and the underlying fixest model in \code{raw.result}.
}
\description{
Runs the full did2s flow: prepare the estimation panel, fit \code{did2s::did2s()}
with unit and time fixed effects in the first stage and event-time dummies in
the second, extract the coefficients, and summarize with the house-style joint
pre-trends F-test and the auxiliary-static-DiD post-treatment average.
}
\details{
The joint pre-trends test uses the full coefficient covariance from the fixest
fit (via \code{\link[=joint_pretrends_F]{joint_pretrends_F()}}); with \code{df2 = Inf} this F p-value equals the
chi-square Wald p-value. The post-treatment average is the auxiliary static
DiD on the never-/not-yet-/treated-in-window subset (see \code{\link[=post_avg_did]{post_avg_did()}}), as
in the BJS estimator. Never-treated units are coded via \code{\link[=is_never_treated]{is_never_treated()}}
(\code{NA}/\code{Inf}/non-positive). The omitted event-study reference is event time -1.
The estimation sample keeps never-treated units and treated observations whose
event time lies in \code{pre.window} or \code{post.window}; treated observations outside
that window are dropped so they do not enter the reference category.
}
\examples{
panel <- readRDS(system.file("extdata", "synthetic_panel.rds",
                             package = "taylorDiD"))
\donttest{
es <- did2s_event_study(panel, "y", "g", "year", "id",
                        pre.window = -4:-1, post.window = 0:3)
es$coefficients
}
}
