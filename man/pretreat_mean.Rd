% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{pretreat_mean}
\alias{pretreat_mean}
\title{Pre-treatment outcome mean for treated units (cohort timing)}
\usage{
pretreat_mean(data, yname, gname, tname)
}
\arguments{
\item{data}{Data frame or data.table.}

\item{yname}{Outcome variable name (string).}

\item{gname}{Treatment-timing (cohort) variable name (string).}

\item{tname}{Time-period variable name (string).}
}
\value{
Numeric scalar, the pre-treatment outcome mean for treated units.
}
\description{
Averages the outcome over treated units' pre-treatment observations, where a
unit is treated when its timing value is a genuine cohort (see
\code{\link[=is_never_treated]{is_never_treated()}}) and an observation is pre-treatment when \code{t < g}. This
is the convention used by the BJS and did2s estimators, which carry cohort
timing in \code{gname}.
}
\examples{
panel <- data.frame(
  y = c(1, 2, 3, 4, 5, 6),
  g = c(2002, 2002, 2002, Inf, Inf, Inf),
  t = c(2000, 2001, 2002, 2000, 2001, 2002)
)
pretreat_mean(panel, "y", "g", "t")
}
