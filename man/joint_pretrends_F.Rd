% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{joint_pretrends_F}
\alias{joint_pretrends_F}
\title{Joint pre-trends F-test}
\usage{
joint_pretrends_F(est, se = NULL, vcov = NULL, n_pre = length(est))
}
\arguments{
\item{est}{Numeric vector of pre-treatment coefficient estimates.}

\item{se}{Numeric vector of standard errors (diagonal approximation). Ignored
when \code{vcov} is supplied.}

\item{vcov}{Optional full covariance matrix of the pre-treatment estimates.}

\item{n_pre}{Number of pre-treatment periods (defaults to \code{length(est)}).}
}
\value{
List with \code{f.stat}, \code{df}, and \code{p.value}.
}
\description{
Reports the F-statistic version of the joint test that all pre-treatment
coefficients are zero: the Wald statistic divided by the number of
pre-periods, with a p-value from \code{pf(F, df1 = n_pre, df2 = Inf)}. When a full
covariance matrix \code{vcov} is supplied the Wald statistic uses it; otherwise it
falls back to the diagonal approximation \code{sum((est / se)^2)} (which is what
the imputation and DIDmultiplegtDYN outputs support, since they expose only
point estimates and standard errors).
}
\examples{
joint_pretrends_F(est = c(0.1, -0.05, 0.02), se = c(0.08, 0.07, 0.09))
}
