% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{did.event.study.plot.dyn}
\alias{did.event.study.plot.dyn}
\title{Event-study plot and LaTeX table (dCDH-style)}
\usage{
did.event.study.plot.dyn(
  es.results,
  model.names = NULL,
  y.titles = NULL,
  decimal.places = 4,
  output.file = NULL
)
}
\arguments{
\item{es.results}{A single \code{taylorDiD_es} object or a list of them.}

\item{model.names}{Optional column/title names (one per model).}

\item{y.titles}{Optional y-axis titles (one per model; default: \code{model.names}).}

\item{decimal.places}{Table precision (default 4).}

\item{output.file}{Optional path for the LaTeX table.}
}
\value{
List with \code{plots} and \code{table}.
}
\description{
Produces a clean event-study plot per model (no caption, with per-model y-axis
titles) and the two-panel LaTeX table, built on \code{\link[=plot_event_study]{plot_event_study()}} and
\code{\link[=tex_event_study_table]{tex_event_study_table()}}.
}
\examples{
es <- new_es_result(
  coefficients = data.table::data.table(
    event.time = -2:2, term = as.character(-2:2),
    estimate = c(0.02, -0.01, 0.30, 0.42, 0.55), std.error = rep(0.1, 5),
    p.value = c(0.8, 0.9, 0.01, 0.001, 0.001),
    conf.low = c(-0.18, -0.21, 0.10, 0.22, 0.35),
    conf.high = c(0.22, 0.19, 0.50, 0.62, 0.75)),
  pre.mean.treated = 2.0,
  pretrends.test = list(f.stat = 0.3, df = 2, p.value = 0.74),
  avg.post.effect = list(estimate = 0.42, std.error = 0.1, p.value = 0.001,
                         pct.change = 21, window = c(0, 2)),
  n.obs = 500L, n.groups = 50L, n.treated = 20L, estimator = "dcdh")
out <- did.event.study.plot.dyn(es, model.names = "Outcome")
}
